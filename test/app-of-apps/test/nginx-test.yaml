apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-test
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    namespace: nginx-test
    name: gke_grpc-exam-475707_asia-northeast3_grpc-demo
  source:
    repoURL: https://github.com/Kyejoon-Lee/test-repo.git
    path: exam
    targetRevision: main
    helm:
      releaseName: nginx-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 10m
    syncOptions:
    - ServerSideApply=false
    - CreateNamespace=true


